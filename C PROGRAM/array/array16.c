//WAP to input any decimal number then convert it into a binary number (use array to store the binary number).
//yet to be solved
#include<stdio.h>
int main(){
    int num;
    int arr[10];
    printf("Enter any number: ");
    scanf("%d",&num);
    for(int i = 0;i<10;i++){
        arr[i] = num%10;
        num/10;
        
    }
    for(int j = 0;j<10;j++){
        arr[j] = 
    }
    return 0;
}